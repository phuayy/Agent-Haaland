> **This pull request was drafted automatically by Agent Haaland and requires human review.**
> It cannot be merged by the automation that opened it — see the repository's
> branch protection rules.

**Incident:** {{ incident.reference }} — {{ dashboard_url }}
**Root cause (AI-stated, confidence {{ "%.0f"|format(confidence * 100) }}%):**
{{ root_cause }}

{{ body_markdown }}

---

### Rollback of this rollback

If this change is wrong, revert this PR's commit — that restores the exact
pre-remediation state. Do not hand-edit; a revert keeps the audit trail
accurate.
